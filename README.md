# Circle Apex Axis

A GitHub Pages site built with Jekyll.

## Local Development

To run this site locally:

```bash
bundle install
bundle exec jekyll serve
```

Then visit `http://localhost:4000` in your browser.

## Publishing

This site is automatically published to GitHub Pages when you push to the `main` branch.

Visit: https://band1eader.github.io/circle-apex-axis
